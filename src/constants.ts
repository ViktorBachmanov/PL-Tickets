export enum RoutesPathes {
    LOGIN = '/',
    NOT_FOUND = 'not_found',
    DASHBOARD = '/dashboard',
    TICKETS = "tickets",
    CREATE = "tickets/new"
};

export enum Priority { LOW, NORMAL, HIGH };